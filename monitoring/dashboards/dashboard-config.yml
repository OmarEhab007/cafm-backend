# Grafana dashboard provisioning configuration
apiVersion: 1

providers:
  - name: 'CAFM Backend Dashboards'
    orgId: 1
    folder: 'CAFM'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards